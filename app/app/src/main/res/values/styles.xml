<resources>

    <!-- Tema base. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cor primaria do aplicativo. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Versão escura da cor primaria para a status bar e contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Cor de acentuação para as views (Checkbox, TextView, etc) -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <!-- Remove a ActionBar -->
        <item name="windowActionBar">false</item>
        <!-- Mantém o título com a Toolbar -->
        <item name="windowNoTitle">true</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Temas para a Toolbar, ver include_toolbar.xml -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Estilos de fonte -->
    <style name="font_26_bold">
        <item name="android:textSize">26sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="font_18_bold">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
