<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cor primaria do aplicativo. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Versão escura da cor primaria para a status bar e contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Cor de acentuação para as views (Checkbox, TextView, etc) -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <!-- Remove a ActionBar -->
        <item name="windowActionBar">false</item>
        <!-- Mantém o título com a Toolbar -->
        <item name="windowNoTitle">true</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Temas para a Toolbar, ver include_toolbar.xml -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="CustomDialogTheme" parent="@android:style/Animation.Dialog">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomDialogTest" parent="@android:style/Theme.Dialog"  >
        <item name="android:windowAnimationStyle">@style/CustomDialogAnimation</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CustomDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_open_animation</item>
    </style>

    <!-- Estilos de fonte -->
    <style name="font_26_bold">
        <item name="android:textSize">26sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="font_18_bold">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="radioButton">
        <item name="android:background">@drawable/teste</item>
        <item name="android:textColor">#595959</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:buttonTint" tools:targetApi="lollipop">#8b8b8b</item>
    </style>



</resources>
